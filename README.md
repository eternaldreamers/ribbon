# Rebbon

Simple dotfile configurator

## Configuration in repo

`$HOME/.config/ribbon/ribbonrc`
 
``` rn
credentials = {
    user = <username>
    pass = <token>
}

origin = {
    ref = heaveless/dotfiles
    branch = termux
}

links = [nvim, zsh]
```
